type: object
title: MultilingualString
description: 'Multilingual string, represented as a pair of attributes, using ISO 639-1:2002 language codes and the translated string'
minProperties: 1
x-tags:
  - common
properties:
  lang:
    type: string
    description: 'The property names should be lower case ISO 639-1:2002 language codes (e.g. en, es, de), and values should hold the respective strings'
    pattern: '^[a-z]{2}'
  string:
    type: string
required:
  - lang
  - string
