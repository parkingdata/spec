title: Equipment
description: 'One type of equipment, which is available on some site, for example on a rest area.'
allOf:
  - $ref: ./EquipmentOrServiceFacility.yaml
  - type: object
    properties:
      electricChargingPoints:
        type: array
        items:
          $ref: ../energy_infrastructure/ElectricChargingPoint.yaml
